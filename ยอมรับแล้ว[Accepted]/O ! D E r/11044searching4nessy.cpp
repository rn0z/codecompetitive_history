#include <bits/stdc++.h>
#include <cstdio>

int main() {
    int tc; scanf("%d", &tc);
    while (tc--) {
        int n, m; scanf("%d %d", &n, &m);
        printf("%d\n",(n / 3) * (m / 3));
    }


    return 0;
}
