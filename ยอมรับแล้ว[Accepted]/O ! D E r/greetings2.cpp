#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    string str; cin >> str;
    int len = str.length() - 2;
    string e(len*2, 'e');
    cout << 'h' + e +'y' << '\n';
    
    return 0;
}
