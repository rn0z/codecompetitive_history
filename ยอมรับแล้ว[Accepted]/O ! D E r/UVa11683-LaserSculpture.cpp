#include <iostream>

using namespace std;

int main() {

    int A, C;
    cin >> A >> C;
    while (A) {
        int count(0); 
        cin >> C;
        while (C--) {
        }
        cin >> A;
    }

    return 0;
}
