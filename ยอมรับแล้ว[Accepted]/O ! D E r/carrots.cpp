#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, p; cin >> n >> p;
    while (n--) {
        string str;
        getline(cin, str);
    }
    cout << p << endl;
    return 0;
}
