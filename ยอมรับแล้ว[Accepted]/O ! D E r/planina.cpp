#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    int n; cin >> n;
    int rs = pow(pow(2, n) + 1, 2);
    cout << rs << '\n';
    
    
    return 0;
}
