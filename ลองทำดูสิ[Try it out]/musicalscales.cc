#include <bits/stdc++.h>

using namespace std;

vector<string> scale{

};

void run_case() {
    int n;
    vector<string> notes{};
    for (auto i = 0; i < n; ++i) {
        string note;
        cin >> note;
        notes.push_back(note);
    }
}

int main() {
    run_case();

    return 0;
}